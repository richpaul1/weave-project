{
  "name": "weave-project",
  "version": "1.0.0",
  "description": "Admin and Client RAG application with comprehensive Weave instrumentation",
  "private": true,
  "workspaces": [
    "admin",
    "admin/client",
    "agent/client"
  ],
  "scripts": {
    "preinstall": "node scripts/check-prerequisites.js || true",
    "install:all": "npm install && cd admin && npm install && cd client && npm install && cd ../../agent && pip install -r requirements.txt && cd client && npm install",
    "dev:admin": "cd admin && npm run dev",
    "dev:agent": "concurrently -n \"agent-be,agent-fe\" -c \"green,yellow\" \"npm run dev:agent-backend\" \"npm run dev:agent-frontend\"",
    "build": "npm run build:admin && npm run build:agent",
    "build:admin": "cd admin && npm run build",
    "build:agent": "cd agent/client && npm run build",
    "test": "npm run test:admin && npm run test:agent",
    "test:admin": "cd admin && npm test",
    "test:agent": "cd agent && python -m pytest tests/",
    "test:coverage": "npm run test:coverage:admin && npm run test:coverage:agent",
    "test:coverage:admin": "cd admin && npm run test:coverage",
    "test:coverage:agent": "cd agent && python -m pytest --cov=app --cov-report=html --cov-report=term",
    "lint": "npm run lint:admin && npm run lint:agent",
    "lint:admin": "cd admin && npm run lint",
    "lint:agent": "cd agent/client && npm run lint",
    "clean": "npm run clean:admin && npm run clean:agent",
    "clean:admin": "cd admin && rm -rf node_modules dist coverage && cd client && rm -rf node_modules dist",
    "clean:agent": "cd agent && rm -rf __pycache__ .pytest_cache htmlcov .coverage && cd client && rm -rf node_modules dist",
    "health": "npm run health:admin && npm run health:agent",
    "check:prereqs": "node scripts/check-prerequisites.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/richpaul1/weave-project"
  },
  "keywords": [
    "rag",
    "llm",
    "weave",
    "wandb",
    "neo4j",
    "fastapi",
    "react",
    "typescript",
    "python"
  ],
  "author": "Your Name",
  "license": "MIT"
}

